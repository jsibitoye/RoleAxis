# RoleAxis Evidence AI Analysis

Service boundary for evidence scoring, chronology extraction, anomaly detection, and case insight generation.

The current FastAPI app keeps first-pass insights in `apps/web/services/insights.py`. This folder is reserved for graduating that logic into a dedicated service as the platform scales.
