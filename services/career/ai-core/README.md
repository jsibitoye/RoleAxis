# RoleAxis Career AI Core

Shared Career workspace AI service boundary for prompt orchestration, model selection, safety rules, and reusable coaching context.

The Interview Assistant currently contains its own desktop AI calls; shared logic should move here when the platform service layer is introduced.
